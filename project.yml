name: WalletBudget
options:
  bundleIdPrefix: com.cadenchiang
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    TARGETED_DEVICE_FAMILY: "1"

targets:
  WalletBudget:
    type: application
    platform: iOS
    sources:
      - path: WalletBudget
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.cadenchiang.walletbudget
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: WalletBudget
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait
        # Allow simulator-SDK compile checks without a signing identity.
        CODE_SIGNING_ALLOWED: NO

  WalletBudgetTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: WalletBudgetTests
    dependencies:
      - target: WalletBudget
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.cadenchiang.walletbudgetTests
        GENERATE_INFOPLIST_FILE: YES
